services:
  - type: web
    name: portfolio-api
    runtime: node
    branch: main
    buildCommand: corepack enable && corepack prepare pnpm@10.34.5 --activate && pnpm install --frozen-lockfile && pnpm prisma migrate deploy && pnpm build
    startCommand: corepack enable && corepack prepare pnpm@10.34.5 --activate && pnpm start:prod
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 24
